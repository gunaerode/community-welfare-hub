# Copy to .env.local for local development.
# Leave empty (or delete) to run as a purely static site — forms go to WhatsApp.
# Set it to your API server to switch on server features (saved forms, admin page, live content).
VITE_API_URL=http://localhost:8787
